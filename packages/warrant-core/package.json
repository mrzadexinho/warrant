{
  "name": "@idriszade/warrant-core",
  "version": "0.1.0",
  "description": "Shared types (ActionRequest, Warrant), Ed25519 signing, and canonicalJson: the single definition of identity for the warrant ecosystem.",
  "private": true,
  "type": "module",
  "license": "MIT",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "typecheck:tests": "tsc -p tsconfig.test.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@idriszade/core": "^0.1.0",
    "@noble/ed25519": "^2.2.3",
    "@noble/hashes": "^1.8.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "fast-check": "^4.4.0"
  }
}
