{
  "name": "@idriszade/warrant-guard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "The enforcement seam. One guard, many actuators: verify, parse, recompute paramsHash, compare, spend the nonce, act, record. Vendor-blind by construction.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "typecheck:tests": "tsc -p tsconfig.test.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@idriszade/core": "^0.1.0",
    "@idriszade/warrant-core": "workspace:*",
    "@idriszade/warrant-ledger": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^4.0.0"
  }
}
