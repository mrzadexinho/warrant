{
  "name": "@idriszade/warrant-gatewerk",
  "version": "0.1.0",
  "description": "The Gate port: how a review reaches a human. Real adapter for Gatewerk plus a deterministic SimGate.",
  "private": true,
  "type": "module",
  "license": "MIT",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "typecheck:tests": "tsc -p tsconfig.test.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@idriszade/core": "^0.1.0",
    "@idriszade/warrant-core": "workspace:*",
    "zod": "^4.4.3"
  }
}
